# Domain whose A record points at this VPS (no trailing slash).
BRIDGE_DOMAIN=tg-api.example.com

# Used only when issuing the Let's Encrypt certificate.
CERTBOT_EMAIL=admin@example.com

# Optional: allow only the Iranian host IP. Leave empty to allow all.
# ALLOW_IRAN_IP=203.0.113.10
